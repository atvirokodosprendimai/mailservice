# TrueVIP Access Mailbox Pulse - 24h - 2026-05-10 10:18 EEST

## Headlines

- Pulse instrumentation is offline: `cmd/pulse` Go binary refuses to start because `TURSO_DATABASE_URL` is empty. Yesterday's scheduled run (the workflow's first ever) failed at 10:17 UTC.
- `gh secret list` confirms `TURSO_DATABASE_URL` and `TURSO_AUTH_TOKEN` were never added to the repo. `POLAR_TOKEN` is set.
- Until DB credentials land, none of the strategy-doc DB metrics can be measured — the pulse skill cannot read anything beyond what is already visible in `STRATEGY.md`.

## Usage

- **Primary engagement (`imap_login`):** no data (instrumentation pending — no log shipper / metrics endpoint on prod, see `memory/project_no_ssh_to_prod.md`)
- **Value realization (`imap_message_fetched`):** no data (instrumentation pending)
- **Completions / conversions:**
  - `mailbox_claimed`: no data (Turso unreachable)
  - `mailbox_activated`: no data (Turso unreachable)
  - `subscription_renewed`: no data (Polar reachable in CI, but pulse binary aborts before Polar query)
- **Strategy metrics:**
  - active_paid_mailboxes (turso): no data — `TURSO_DATABASE_URL` missing
  - claim_to_activation_conversion (turso): no data — `TURSO_DATABASE_URL` missing
  - monthly_renewal_rate (polar): no data — pulse binary fails before reaching Polar step
  - resolve_calls_per_active_mailbox_per_week: no data (instrumentation pending)
  - failed_key_proof_ratio: no data (instrumentation pending)

## System performance

Omitted. `pulse_tracing_source` is set to `omit`; no Coroot/Sentry/Datadog reaches truevipaccess prod yet (Coroot lives on smoke only).

## Followups

- Add `TURSO_DATABASE_URL` and `TURSO_AUTH_TOKEN` GitHub Action secrets, scoped read-only. Without these the daily pulse will keep failing in 0.04s.
- Decide whether `cmd/pulse` should fail-closed on missing Turso (current behavior) or fall back to a Polar-only run; today, even the working POLAR_TOKEN never gets used.
- Land `resolve_calls` and `edproof_failed` log emission + a remote sink (HTTP push from Postfix-side handler, or `/admin/metrics` endpoint), since prod has no SSH and journald is host-local.
- Backfill `pulse_lookback_default` query plan once a sink exists, then re-run `/pulse` to confirm the report renders end-to-end.

---
_Source windows: analytics [n/a], tracing [n/a], payments [unreached due to upstream failure]. Trailing buffer: 15m. Saved to `docs/pulse-reports/2026-05-10_10-18.md`._
